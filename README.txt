To execute:
airTunnel -system <1,2> -radius <r1> <r2> -mass <m1> <m2>

-system: 1 is single Ball object, 2 is 2 Ball Objects.

The radii of the balls are set by default at 0.5 and 0.6 respectively,
masses are both 50.0 by default 